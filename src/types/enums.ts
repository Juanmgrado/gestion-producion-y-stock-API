export enum ProductSortBy {
  NAME = "name",
  STOCK = "stock",
  IS_ACTIVE = "isActive",
  CREATED_AT = "createdAt",
}

export enum Order {
  ASC = "ASC",
  DESC = "DESC",
}

export enum UserSortBy {
  NAME = "name",
  EMAIL = "email",
  CODE = "code",
}

export enum MovementType {
  IN = "IN",
  OUT = "OUT",
}
